services:
  - type: worker
    name: yoasobi-checker
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
  - fromGroup: yoasobi-secret

      

